/*
Creating an Array

Complete a function that takes no parameters, and returns a newly 
created array. Your function should create a variable and assign it to 
a new array, then return that variable. 
*/

function createArray() {
    // create a result variable,
    // assign it to a new array
    // return the result variable

    return [];
}

var resultArray = createArray();
console.log('should be an array:', Array.isArray(resultArray));