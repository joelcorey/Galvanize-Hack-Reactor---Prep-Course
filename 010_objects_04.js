/*
Write a function that returns the type of argument the function has 
been called with (assume the argument will be scalar - not a collection).
Scalar means one value at a time.
*/

function getType(param) {
    // returns the type of param
    return typeof param
}