/*
complete a function that takes in an object and a string (will 
refer to a key in the object). The function should return the value of 
the property located within the object at the given string key.
*/

function returnAValue(obj, key) {
    // returns value of inputted object's property located at key
    return obj[key]
}