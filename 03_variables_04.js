/* 
Lastly, we are going to complete a function that takes two parameters, 
a string and number (will refer to an index within the string). 
The function should return the character within the string, located 
at the given number index.
*/

function returnACharacter(string, index) {
	return string[index]
}